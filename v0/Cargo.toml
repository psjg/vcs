[package]
name = "v0"
version = "0.1.0"
edition = "2024"
description = "A version control spike: patch theory over a CRDT op log"

[dependencies]
blake3 = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
similar = "3.2.0"

[dev-dependencies]
proptest = "1"
